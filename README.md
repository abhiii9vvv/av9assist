# av9Assist

AI chat assistant built with Next.js.

## Quick start

- Install dependencies: `pnpm install`
- Run dev server: `pnpm dev`
- Build: `pnpm build`

## Notes

- App Router structure in `app/`.
- Configuration in `next.config.mjs` and `tailwind.config.js`.
